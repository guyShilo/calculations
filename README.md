# This readme will tell you how to run the app

## Running with docker
Execute `npm run with-docker` from the root of the repository
## Running locally
Execute `npm run start-local` from the root of the repository

